@model IEnumerable<MRP_Ratboy.Models.Perfil_Ensamble>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMenuAdmin.cshtml";
}

<h2>Perfil - Ensamble</h2>

<p class="btn btn-info" style="cursor:pointer;">
    @Html.ActionLink("NUEVO", "Create", null, 
     new { @style="text-decoration: none; color: #fff;" })
</p>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ensamble.idEnsamble)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Perfiles.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nivel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.estatus)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ensamble.idEnsamble)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Perfiles.Nombre)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.idPerfil_Ensamble }, new { @class="btn btn-info" }) |
                    @Html.ActionLink("Detalles", "Details", new { id = item.idPerfil_Ensamble }, new { @class="btn btn-info" }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.idPerfil_Ensamble }, new { @class="btn btn-danger" })
                </td>
            </tr>
        }

</table>
