@model IEnumerable<MRP_Ratboy.Models.modeloVideo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.modelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tarjetaVideo.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.modelo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tarjetaVideo.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idModeloVideo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idModeloVideo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idModeloVideo })
        </td>
    </tr>
}

</table>
