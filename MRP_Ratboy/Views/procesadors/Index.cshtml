@model IEnumerable<MRP_Ratboy.Models.procesador>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutMenuAdmin.cshtml";
}

<h2>Procesadores</h2>

<p class="btn btn-info" style="cursor:pointer;">
    @Html.ActionLink("NUEVO", "Create", null, 
     new { @style="text-decoration: none; color: #fff;" })
</p>
<table class="table">
    <thead class="thead-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.costoVenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.marca)
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.watts)
            </th>
            <th>
                Detalle generación
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.tipoMemoria.tipo)
            </th>
            <th></th>
        </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nombre)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.costoVenta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.marca)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.watts)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.detalleGeneracionProcesador.DetalleGeneracionProcesador1)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.tipoMemoria.tipo)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.idProcesador }, new { @class="btn btn-info" }) |
                    @Html.ActionLink("Detalles", "Details", new { id = item.idProcesador }, new { @class="btn btn-info" }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.idProcesador }, new { @class="btn btn-danger" })
                </td>
            </tr>
        }

</table>
